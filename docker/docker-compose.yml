version: '3.8'

services:
  php:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    ports:
      - "9000:9000"
    command: ["php-fpm", "-F"]

  nginx:
    build:
        context: nginx
        dockerfile: Dockerfile
    ports:
        - "8190:80"



